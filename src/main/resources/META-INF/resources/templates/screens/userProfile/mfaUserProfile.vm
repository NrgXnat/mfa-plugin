<script type="text/javascript" src="$content.getURI("/scripts/mfa/multifactorAuth.js")"></script>
<link ref="stylesheet" type="text/css" href="$content.getURI("/style/mfa/multifactorAuthUserProfile.css")" />
<script type="text/javascript">

$(document).ready(function() {
		var USERNAME = '#escapeAll($!item.getProperty("login"))';
		var multifactorAuthTab = {
			kind: 'tab',
			label: 'Multifactor Authentication',
			group: 'General',
			contents: {
			    multifactorAuthPanel: {
				kind: 'panel',
				label: 'Multifactor Authentication Settings',
				footer: false,
				contents: {
				    authenticatorSetup:{
					tag: 'div#authenticator-setup',
					contents:{
						authenticatorMethod: {
						    tag:'div#authenticator-method'
						},
						authenticatorExempted: {
						    tag:'div#authenticator-exempted'
						},
						authenticatorRegistered: {
						    tag:'div#authenticator-registered'
						}
					}
				    }
			       }
			  }
		      }
		    }

   		    XNAT.spawner.spawn({multifactorAuthTab:multifactorAuthTab}).render($("div#user-profile-manager>div.xnat-tab-content"));
		    XNAT.app.MultifactorAuth.renderUserProfile();

});
</script>